{
  "name": "gulp-static-from-php",
  "version": "0.0.2",
  "description": "A static site generator that uses php to render pages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Arjen Smit",
  "license": "MIT",
  "dependencies": {
    "gulp-util": "^3.0.7",
    "shelljs": "^0.6.0",
    "through2": "^2.0.1"
  }
}
